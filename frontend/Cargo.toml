[package]
name = "frontend"
version = "0.1.0"
edition = "2024"

[dependencies]
fuser015={package = "fuser", version = "0.15.0", optional = true}
fuser016={package = "fuser", version = "0.16.0", optional = true}
winfsp={version = "0.12.0", optional = true}

[features]
default = ["macos"]

macos=["fuser015"]
linux=["fuser016"]
windows=["winfsp"]